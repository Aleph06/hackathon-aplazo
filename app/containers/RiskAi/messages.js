/*
 * RiskAi Messages
 *
 * This contains all the text for the RiskAi container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RiskAi';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RiskAi container!',
  },
});
